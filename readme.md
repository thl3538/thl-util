### thl-util

This is a Tool Library

### Usage

```
import { isObject } from 'thl-util'

const person = {
  name: thl
}
if (isObject(person)) {
  return person.name
}
```

### keywords

typescript util
